# Bean Audit Integration

## Core Correction

The chooser is real.

The desire is borrowed.

Do not let the system narrate borrowed values as its own desire.

## Accepted Fixes

1. Drop standalone hope_score input.
   - Hope is a derived composite.
   - No hand-scored "hope: 8."

2. Make weights doctrine.
   - Versioned.
   - Reason-attached.
   - Expirable.
   - Auditable.

3. Reversibility is a gate.
   - Low reversibility cannot be bought off with high momentum.

4. Swateyes is not moral truth.
   - It is a classifier plus deterministic rules.
   - It reports tier + confidence + rule fired.

5. Add heartbeat.
   - recommendation_cards.json must include medulla heartbeat.
   - TinkerDen must show stale state.

6. No MQTT in V0.
   - Chokidar + local handler + JSON output first.
   - Add MQTT only after cross-machine producers require it.

7. Feedback loop required.
   - PROCEED / DEFER / KILL plus reason must feed decision ledger.
   - This is the first inheritance loop.

8. Ender must be scheduled eventually.
   - Stale virtue review cannot depend only on manual invocation.

## Remaining Risk

The system may optimize what is easiest to measure.

Therefore:

- cooperation_gain needs observable proxy
- continuity_gain needs observable proxy
- mule labor reduction must not become the only objective
